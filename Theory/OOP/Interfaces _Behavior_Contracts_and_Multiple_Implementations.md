Интерфейсы (Interfaces) — "Договор о намерениях"

1. Механика Контракта — Связь "Умею делать"

Суть: Если наследование (классы) — это родство (кто ты?), то интерфейс — это навык (что ты умеешь?). 
Интерфейс не содержит данных, он содержит только список правил, которые класс обязан выполнить.


Interface: Это «пустой бланк». Класс говорит: «Я подписываюсь под этим контрактом и обещаю, что у меня будут вот такие методы».

Implementation (Реализация): Когда класс пишет : IMyInterface, он берет на себя обязательство написать тело для каждого метода из этого интерфейса.

Синтаксис: Название всегда начинается с буквы I (IStore, IWorkable). Ключевое слово interface.

Пример:



    C#
    
    
    
    public interface IDamageable
    
    {
    
        // Никаких полей, никаких public/private. Просто план.
    
        void TakeDamage(int amount); 
    
    }public class Warrior : IDamageable 
    
    {
    
        private int _health = 100;
    
        
    
        // Обязаны реализовать метод из интерфейса
    
        public void TakeDamage(int amount) 
    
        {
    
            _health -= amount;
    
            Console.WriteLine($"Воин ранен! Осталось: {_health}");
    
        }
    
    }

2. Множественная реализация — "Мастер на все руки"

Суть: В C# класс может наследоваться только от одного родителя, но может реализовывать сколько угодно интерфейсов.

Это позволяет собирать объект как конструктор из разных умений. 
Например, робот может быть одновременно IDriveable (управляемым) и IChargeable (заряжаемым).



    C#
    
    
    
    public class CombatRobot : IRobot, IDamageable, IWeapon
    
    {
    
        // Реализуем методы всех трех интерфейсов в одном классе
    
    }

3 Абстрактный класс: «Кем быть» (Сущность)
Это фундамент и родословная.

Связь (Is-a): Объект является частью определенной семьи.

Например: PremiumSubscription — это прежде всего Subscription.

Наследование: Строгая иерархия. Ты можешь иметь только одного «отца» (базовый класс). 
В C# запрещено множественное наследование классов.

Данные: Настоящий склад. Может хранить поля (private int _price), состояния и общие переменные, которые унаследуют все дети.

Логика: Содержит «золотой стандарт» поведения. 
Ты можешь написать готовый метод с кодом, и наследники будут его использовать «как есть», пока не захотят переопределить.

?? Интерфейс: «Что делать» (Способность)
Это контракт и набор навыков.

Связь (Can-do): Объект умеет выполнять определенные действия, независимо от того, что он такое.

Например: И подписка, и кассовый чек могут быть IPrintable (умеют печататься).

Наследование: Полная свобода. Твой класс может быть кем-то одним, но при этом обладать сотней навыков (реализовывать сколько угодно интерфейсов).

Данные: Это «пустой бланк». Интерфейс не может хранить данные (поля).
Он лишь говорит: «У тебя должно быть свойство X», но где ты будешь его хранить — твоя забота.

Логика: Это чистый план. До последних версий C# интерфейсы вообще не могли содержать код — только заголовки методов. 
Это чистая декларация намерений.

Главный итог:
Выбирай Абстрактный класс, если у тебя есть группа объектов с общими корнями и ты хочешь дать им готовый код и общие данные.

Выбирай Интерфейс, если ты хочешь объединить совершенно разные объекты по их умениям 
(например, Plane, Bird и Kite — все они умеют IFlyable, но они не родственники).

Понял тебя. Давай возьмем те 4 глубокие темы, которые мы обсудили 
(Явная реализация, Коллекции, DI и Методы по умолчанию), и объединим их в единую архитектурную картину с примерами кода.

🛠 4 Столпа Продвинутой Архитектуры
1. Явная реализация (Explicit Implementation)
Суть: Метод интерфейса «прячется» внутри класса. Он недоступен через обычную переменную, только через интерфейсную «маску».

Зачем: Разделение ролей. Менеджер как Сотрудник просто получает зарплату, но как Лидер он раздает задачи. 
Мы не хотим, чтобы метод «Раздать задачи» мешался в обычном списке дел.

    C#
    // В классе Manager:
    void ILeader.GiveTask(IWorker worker, string taskName) 
    {
        // Метод скрыт. Чтобы его вызвать, нужно: ((ILeader)manager).GiveTask(...)
        worker.TakeTask(taskName); 
    }
2. Системные интерфейсы (IEnumerable, IReadOnlyList)
Суть: Использование самых «слабых» интерфейсов для передачи данных.

Зачем: Безопасность и гибкость. Если метод просит IEnumerable, ему можно подсунуть и List, и Array, и Queue. 
При этом метод сможет только «читать» данные, но не портить их (удалять или добавлять).

    C#
    public class Company
    {
        private readonly List<IEmployee> _employees;
        
        // Мы отдаем IEnumerable. Снаружи никто не сможет сделать .Add() или .Clear()
        public IEnumerable<IEmployee> Employees => _employees; 
    }
3. Внедрение зависимостей (Dependency Injection — DI)
Суть: Объект не создает своих «помощников» сам, а получает их снаружи в готовом виде.

Зачем: Универсальность. Твой сервис выплаты зарплат не «прибит гвоздями» к конкретному списку людей. 
Он работает с любым объектом, который соблюдает контракт.

    C#
    public class PayRollService
    {
        // DI через параметр метода: сервис зависит от интерфейса IEmployee, 
        // а не от конкретного класса Freelancer или Manager.
        public void ProcessSalary(IEmployee employee) 
        {
            employee.CalculateSalary(); 
        }
    }
4. Методы интерфейсов по умолчанию (Default Methods)
Суть: Возможность написать логику прямо в интерфейсе.

Зачем: Избегание дублирования. Если действие у всех одинаковое (например, вывод имени), зачем писать его 10 раз в каждом классе?

    C#
    public interface IEmployee 
    {
        string Name { get; }
        // Реализация по умолчанию. Классам реализовывать не обязательно!
        void GetInfo() => Console.WriteLine($"Сотрудник: {Name}");
    }
🔗 Как они работают вместе? (Единый пример)
Представь, что мы собираем всё это в одну цепочку:

Мы создаем список сотрудников и внедряем (DI) его в Company.

Company хранит их как IEnumerable (защита данных).

Когда мы обходим список, мы вызываем метод по умолчанию GetInfo().

Если сотрудник оказывается Лидером, мы делаем приведение и вызываем явно реализованный метод GiveTask().

    C#
    // Итоговый цикл в Main, где всё соединилось:
    foreach (var emp in company.Employees) // Работа с IEnumerable
    {
        emp.GetInfo(); // Вызов метода по умолчанию
        
        if (emp is ILeader leader) // Проверка интерфейса
        {
            leader.GiveTask(someWorker, "План"); // Работа через явную реализацию
        }
    }

Правила Проектирования
Принцип «Пульта»: Интерфейс — это только кнопки. 
Тебе не важно, как работает механизм внутри телевизора или плеера, пока на пульте есть кнопка Play.

Граница контракта: Мы никогда не лезем во «внутренности» класса. 
Общение происходит только через методы и свойства, прописанные в интерфейсе.

Чистота данных: В интерфейсах — только свойства (Properties) и методы. Никаких полей (fields). 
Данные — это тайна класса, скрытая за private.

2. Стратегия Взаимодействия
Минимальное знание (Loosely Coupling): Не проси конкретный класс (FileLogger), если тебе нужно просто записать строку.
Проси интерфейс (ILogger). Тебе должно быть плевать на реализацию.

Инверсия зависимости: Твой код должен зависеть от абстракций (интерфейсов), а не от конкретных «железных» классов. 
Это позволит на 16-й неделе заменить БД, не трогая логику.

3. Маркеры Успеха
Сигнал «Скука»: Если интерфейс кажется тебе просто «скучным списком заголовков», поздравляю — ты понял его суть. Это обязательство, а не творчество.

Тест на IVolumeControl: Можешь применить один метод ChangeVolume к TV и Radio? Если да — полиморфизм у тебя в крови.
